{{!-- filepath: c:\Users\georg\Documents\_uni\8sem\web_programming\project\git\web_project\view\set_event.hbs --}}
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{pageTitle}}</title>
  <link rel="stylesheet" href="/styles.css">
  <style>
    .event-form-container {
      max-width: 600px;
      margin: 40px auto;
      background: #fff;
      padding: 30px;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }
    .event-form-container h2 {
      text-align: center;
      margin-bottom: 20px;
      color: #333;
    }
    .form-group {
      margin-bottom: 15px;
    }
    .form-group label {
      display: block;
      margin-bottom: 5px;
      font-weight: 600;
      color: #333;
    }
    .form-group input,
    .form-group textarea,
    .form-group select {
      width: 100%;
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 4px;
      font-size: 16px;
    }
    .optional {
      font-size: 14px;
      color: #777;
    }
    button[type="submit"] {
      width: 100%;
      padding: 12px;
      background: #4a90e2;
      color: #fff;
      font-size: 18px;
      border: none;
      border-radius: 4px;
      cursor: pointer;
    }
    button[type="submit"]:hover {
      background: #357ab7;
    }
  </style>
</head>
<body>
  <div class="event-form-container">
    <h2>Δημιουργία Εκδήλωσης</h2>
    <form class="signup-form" action="/set-event" method="POST" enctype="multipart/form-data">
      
      <div class="form-group">
        <label for="title">Όνομα Εκδήλωσης</label>
        <input type="text" id="title" name="title" placeholder="Εισάγετε το όνομα της εκδήλωσης" required>
      </div>
      
      <div class="form-group">
        <label for="event-date">Ημερομηνία</label>
        <input type="date" id="event-date" name="event_date" required>
      </div>
      
      <div class="form-group">
        <label for="event-time">Ώρα</label>
        <input type="time" id="event-time" name="event_time" required>
      </div>
      
      <div class="form-group">
        <label for="city">Πόλη</label>
        <select id="city" name="city" required>
          <option value="">Επιλέξτε πόλη</option>
          {{#each cities}}
            <option value="{{this}}">{{this}}</option>
          {{/each}}
        </select>
      </div>
      
      <!-- Single space dropdown -->
      <div class="form-group">
        <label for="event-space">Χώρος</label>
        <select id="event-space" name="event_space" required>
          <option value="">Επιλέξτε χώρο</option>
          {{#each spaces}}
            <option data-city="{{location}}" value="{{id}}">{{name}}</option>
          {{/each}}
        </select>
      </div>

      <!-- Static zone pricing inputs (ticket price per zone) -->
      <div class="form-group">
        <label for="price-zoneA">Τιμή για Zone Α (γραμμές 1-3)</label>
        <input type="number" id="price-zoneA" name="price_zoneA" placeholder="Εισάγετε τιμή για Zone Α" required>
      </div>
      <div class="form-group">
        <label for="price-zoneB">Τιμή για Zone Β (γραμμές 4-6)</label>
        <input type="number" id="price-zoneB" name="price_zoneB" placeholder="Εισάγετε τιμή για Zone Β" required>
      </div>
      <div class="form-group">
        <label for="price-zoneC">Τιμή για Zone C (γραμμές 7+)</label>
        <input type="number" id="price-zoneC" name="price_zoneC" placeholder="Εισάγετε τιμή για Zone C" required>
      </div>
      
      <!-- Remove the generic ticket price field -->
      
      <div class="form-group">
        <label for="event-category">Κατηγορία Εκδήλωσης</label>
        <select id="event-category" name="event_category" required>
          <option value="">Επιλέξτε κατηγορία</option>
          {{#each categories}}
            <option value="{{this}}">{{this}}</option>
          {{/each}}
        </select>
      </div>
      
      <div class="form-group">
        <label for="event-description">Περιγραφή</label>
        <textarea id="event-description" name="event_description" placeholder="Πληροφορίες για την εκδήλωση" rows="5" required></textarea>
      </div>
      
      <div class="form-group">
        <label for="event-image">Εικόνα Εκδήλωσης <span class="optional">(προαιρετικά)</span></label>
        <input type="file" id="event-image" name="event_image" accept="image/*">
      </div>
      
      <button type="submit">Δημιουργία Εκδήλωσης</button>
    </form>
  </div>
  <script src="/src/setEvent.js"></script>
</body>
</html>